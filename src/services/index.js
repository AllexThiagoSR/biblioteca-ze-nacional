const bookService = require('./book.service');
const userService = require('./user.service');
const rentalService = require('./rental.service')

module.exports = { bookService, userService, rentalService };
